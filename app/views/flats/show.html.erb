<div class="banner" style="background-image: url(<%= @flat.banner_url %>)">
  <div class="container">
    <h1><%= @flat.name %></h1>
    <p><%= @flat.description %></p>
    <a class="btn btn-flat" href="#">Apply now</a>
  </div>
</div>

  <div class="container">
    <div class="row">
        <div class="col-3">
          <div class="card-trip">
            <img src= "<%= @flat.banner_url %>" />
            <div class="card-trip-infos">
              <div>
                <h2><%= @flat.name %></h2>
                <p><%= @flat.address %></p>
              </div>
              <h6 class="card-trip-pricing">$<%= @flat.price_per_night %></h4>
            </div>
          </div>
        </div>
    </div>
  </div>
